principal(){
  inteiro !a;
  !a = (1 + (2 * 3) - (4 / 2));
  escreva("Resultado: ", !a);
  se (!a > 0) {
    texto !msg[10];
    !msg = "Positivo";
    escreva(!msg);
  } senao {
    escreva("Nao positivo");
  }
  // Comentario: Colchetes em declaracao e parenteses em expressoes
  decimal !d[2.2];
  !d = 10.5;
}
